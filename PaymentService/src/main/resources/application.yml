spring:
  application:
    name: PaymentService
  config:
    import: optional:configserver:http://config-server:8888
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 10 # Số lần retry tối đa
        initial-interval: 2000 # Thời gian chờ trước lần retry đầu tiên (ms)
        multiplier: 1.5 # Hệ số nhân giữa các lần retry
        max-interval: 10000

logging:
  level:
    root: INFO
  appender:
    logstash:
      destination: logstash:5000

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka

paypal:
  client:
    id: AcbmqATxBNYSNscNEdozkx40CnBkjNSsNpwiF2vpvM-GccRXEUg3o2fCmssSGsWyk6klbEAm5AFaivNs
    secret: EJ4g2fCwRj7YrjWyGG9CX2uqar1mugs7wyed4tDG2CzpxuhF7x5JUP2-BbZos-JYC4p3G-pkdH2RA8Nm
  mode: sandbox
  SUCCESS_URL: http://localhost:8004/payment/success
  CANCEL_URL: http://localhost:8004/payment/cancel

application:
  config:
    order-url: http://gateway:8222/orders/orders