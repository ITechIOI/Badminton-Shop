spring:
  datasource:
    url: jdbc:mysql://userservice-nguyenloan2000hbt-d1f9.h.aivencloud.com:14850/userservice?sslMode=REQUIRED
    username: avnadmin
    password: AVNS_mnCRriUO6Ub4Xcbz81e
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: e5730f994d0ef2
    password: ec10874e2fbfb3
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8
  kafka:
    consumer:
      bootstrap-servers: kafka:9092
      group-id: orderGroup, paymentGroup, productGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        spring.json.type.mapping: orderResponse:com.example.notificationservice.modules.feign.Orders.OrderResponse, paymentResponse:com.example.notificationservice.modules.feign.Payments.PaymentResponse, productResponse:com.example.notificationservice.modules.feign.Payments.PaymentResponse

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://keycloak:8080/realms/micro-service"
          jwk-set-uri: http://keycloak:8080/realms/micro-service/protocol/openid-connect/certs

server:
  port: 8005

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
