spring:
  datasource:
    url: jdbc:mysql://productservice-hoanhngoc2807-f1d6.h.aivencloud.com:25022/productservice?sslMode=REQUIRED
    username: avnadmin
    password: AVNS_qrcfGd_WdD7hIkLx87j
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: paymentResponse:com.example.productservice.modules.feign.Payments.PaymentResponse

    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        spring.json.type.mapping: paymentResponse:com.example.productservice.modules.feign.Payments.PaymentResponse

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:9098/realms/micro-service"
          jwk-set-uri: http://localhost:9098/realms/micro-service/protocol/openid-connect/certs

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8002

application:
  config:
    user-url: http://localhost:8222/users/users
    order-details-url: http://localhost:8222/orders/order-details

cloudinary:
  cloud-name: daxxuqogj
  api-key: 436316462347214
  api-secret: 5oIMlaDECvys_-OGGZt-Irk8_Hw